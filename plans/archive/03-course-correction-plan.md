# 🔧 خطة تصحيح المسار (Course Correction Plan)

> **تاريخ الإنشاء:** 2026-01-26  
> **السبب:** الانحراف عن الرؤية الأصلية بإنشاء نافذة محرر منفصلة  
> **الهدف:** تحويل البرنامج لنافذة موحدة تتحول كما هو موثق في `PRODUCT_VISION.md`

---

## 📋 قائمة المهام الكاملة (TODO List)

### المرحلة 0: الإعداد والتنظيف 🧹

- [x] **0.1** إيقاف التطبيق الحالي ✅
- [x] **0.2** إنشاء نسخة احتياطية (`Backups/v0.1.1_before_refactor/`) ✅
- [x] **0.3** قراءة ومراجعة `docs/PRODUCT_VISION.md` للتأكد من الفهم ✅

---

### المرحلة 1: إزالة النافذة المنفصلة 🗑️

- [x] **1.1** حذف تعريف نافذة `editor` من `tauri.conf.json` ✅
  - ملف: `src-tauri/tauri.conf.json`
  - الإجراء: حذف الـ object الخاص بـ `label: "editor"`

- [x] **1.2** حذف Route `/editor` من `App.tsx` ✅
  - ملف: `src/App.tsx`
  - الإجراء: إزالة أي كود متعلق بـ `window.label === 'editor'`

- [x] **1.3** تعديل `toggle_editor` command في Rust ✅
  - ملف: `src-tauri/src/lib.rs`
  - الإجراء: تحويله لـ event يُرسل للـ Frontend بدلاً من فتح نافذة

- [x] **1.4** اختبار: التأكد من عدم ظهور نافذة منفصلة عند التشغيل ✅
  - تشغيل `npm run tauri dev`
  - التأكد من وجود نافذة واحدة فقط

---

### المرحلة 2: تحويل Toolbar لـ Component متحول 🔄

- [ ] **2.1** إنشاء State للتحكم في وضع النافذة
  - ملف: `src/App.tsx` أو `src/stores/appStore.ts`
  - State: `mode: 'toolbar' | 'editor'`

- [ ] **2.2** تعديل `Toolbar.tsx` ليدعم وضعين
  - وضع الشريط: التصميم الحالي
  - وضع الـ Header: نفس الأزرار + أدوات المحرر

- [ ] **2.3** إنشاء Component `EditorToolbar.tsx`
  - أدوات: عين (معاينة/تحرير)، Load، Copy HTML، Snap/Resize
  - يظهر فقط في وضع المحرر

- [ ] **2.4** إنشاء Component `EditorContent.tsx`
  - يحتوي على Textarea (وضع التحرير) أو HTML Preview (وضع المعاينة)
  - State: `viewMode: 'edit' | 'preview'`

- [ ] **2.5** تعديل Layout في `App.tsx`
  ```
  if (mode === 'toolbar') {
    return <Toolbar orientation={...} />
  } else {
    return (
      <div className="editor-panel">
        <Toolbar orientation="horizontal" /> {/* Header */}
        <EditorToolbar />
        <EditorContent />
      </div>
    )
  }
  ```

- [ ] **2.6** اختبار: الضغط على M يحول الشريط للوحة

---

### المرحلة 2.5: تحسينات الواجهة وتوحيد الهوية (UI Polish) 🎨

- [ ] **2.5.1** دمج الشريط (Header) مع المحرر بصرياً
  - إزالة `borderRadius` والحواف الدائرية عندما نكون في وضع `editor`
  - توحيد لون الخلفية لتبدو متصلة (Header Seamless Blending)
  - التأكد من إزالة أي فواصل أو ظلال تفصل الهيدر عن المحرر

- [ ] **2.5.2** نظام الثيمات (Themes)
  - زر جديد بجانب "العين" لتغيير الثيم (ضغطة طويلة أو قائمة منسدلة)
  - الأوضاع: داكن (Dark)، فاتح (Light)، دافئ (Warm - Sepia)
  - حفظ الثيم المختار في الإعدادات

---

### المرحلة 3: تفعيل أدوات المحرر (Editor Toolbar) �️

- [ ] **3.1** تفعيل زر المعاينة (Preview Toggle)
- [ ] **3.2** تفعيل Load from Clipboard
- [ ] **3.3** تفعيل Copy HTML
- [ ] **3.4** قائمة Snap/Resize (Windows 11 Style)

---

### المرحلة 4: حفظ الحالة (Persistence) 💾

- [ ] **4.1** حفظ آخر موضع
- [ ] **4.2** حفظ آخر ثيم مستخدم

---

### المرحلة 5: الاختبار النهائي والقبول (UAT) ✅

- [ ] اختبار الوظائف الأساسية (Copy, Paste, History)
- [ ] اختبار Focus Guardian
- [ ] اختبار Smart Scrub
- [ ] اختبار Markdown Editor (فتح، إغلاق، تغيير حجم)
- [ ] اختبار الثيمات
- [ ] اختبار الأداء (Memory Leak Check)

---

## 📊 التقدم

| المرحلة | الحالة | ملاحظات |
|---------|--------|---------|
| 0. الإعداد | ⬜ لم يبدأ | |
| 1. إزالة النافذة المنفصلة | ⬜ لم يبدأ | |
| 2. Toolbar متحول | ⬜ لم يبدأ | |
| 3. حجم ديناميكي | ⬜ لم يبدأ | |
| 4. أدوات المحرر | ⬜ لم يبدأ | |
| 5. التحول التلقائي | ⬜ لم يبدأ | |
| 6. تذكر الموضع | ⬜ لم يبدأ | |
| 7. الاختبار | ⬜ لم يبدأ | |
| 8. التوثيق | ⬜ لم يبدأ | |

---

## ⚠️ قواعد التنفيذ

1. **لا تعديل بدون موافقة:** أي تغيير يتطلب موافقة كتابية من المطور.
2. **خطوة بخطوة:** تنفيذ مهمة واحدة ثم اختبار ثم الانتقال للتالية.
3. **المرجع:** `docs/PRODUCT_VISION.md` هو المصدر الوحيد للحقيقة.
4. **التوثيق أولاً:** قبل كتابة الكود، تأكد من فهم المطلوب.

---

**المطور:** احمد صالح  
**المنفذ:** [سيُملأ أثناء التنفيذ]
